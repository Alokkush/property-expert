rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to properties collection for authenticated users
    match /properties/{document} {
      allow read, write: if request.auth != null;
    }
  }
}